let Constants = {
  //  install
  install(Vue, options) {
    //  create global Constants
    Vue.Constants = {
    	MULTILINE_TEXT_ID_PREFIX: "multiline-text"
    };
  }
};

export default Constants;